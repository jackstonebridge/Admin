angular.module('proton.utils', [
    // 'proton.constants'
])
    // .run((eventManager, cache, cacheCounters, logoutManager) => {
    //     logoutManager.attach(() => {
    //         // Stop event manager request
    //         eventManager.stop();
    //         // Clear cache
    //         cache.reset();
    //         cacheCounters.reset();
    //     });
    // });
